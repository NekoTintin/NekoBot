import discord
from discord import app_commands
from discord.ext import commands

class Sysmsg(commands.Cog):
    
    def __init__(self, bot) -> None:
        self.bot = bot
        
    @app_commands.command(name="msg")
        
async def setup(bot) -> None:
    await bot.add_cog(Sysmsg(bot))